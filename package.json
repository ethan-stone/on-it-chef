{
  "name": "on-it-chef",
  "version": "0.0.0",
  "scripts": {
    "build": "sst build",
    "console": "sst console",
    "deploy": "sst deploy",
    "dev": "sst dev",
    "remove": "sst remove",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@tsconfig/node22": "^22",
    "typescript": "^5"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "sst": "^3"
  }
}
