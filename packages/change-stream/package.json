{
  "name": "@on-it-chef/change-stream",
  "type": "module",
  "version": "0.0.0",
  "scripts": {
    "build": "bun build ./src/index.ts --outdir dist --target bun",
    "start": "sst shell bun run dist/index.js",
    "dev": "bun run src/index.ts",
    "example": "bun run src/example.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.864.0",
    "@aws-sdk/client-sns": "^3.864.0",
    "@on-it-chef/core": "workspace:*",
    "mongodb": "^6.17.0"
  },
  "devDependencies": {
    "@types/bun": "^1.2.20",
    "@types/node": "^24.0.3",
    "typescript": "^5.0.0"
  }
}
